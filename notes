jika pada web security
csrf nya aktif dan menggunakan csrf.csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
maka akan spring akan mencatat 'x-xsrf-token' di cookies spring backend
jika csrf nya aktif dan tidak menggunakan .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse()) maka spring hanya akan mencatat 'session' spring banckend saja
kamu bisa cek cookie dari spring dan intellj di http-client.cookies di folder <root user>/.idea/http-client.cookies

perbedaan X-CSRF dan X-XSRF adaalah X-CSRF itu didapat dari response API sedangkan XSRF itu tidak perlu bisanya di https
